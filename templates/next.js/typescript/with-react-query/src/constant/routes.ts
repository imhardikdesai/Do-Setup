// Note: Routes which will be only accessible by guest
export const GUEST_ROUTES: string[] = ['/login', '/signup']

// Note: Routes which will be accessible by authenticated and guest users
export const AUTH_ROUTES: string[] = []

// Note: Routes which is not declared here that will be accessible by authenticated users only

// ex: "/dashboard", "/profile/:id", "/settings/:section"
